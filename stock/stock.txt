计算60天的数据
    从第一天算起，

    首次，生成100*100的数组，

    然后扫描数据，计算百分比
        第一个数据为参考，后面的数据以此为参考，计算百分比，每日数据将形成4个点的数据，新new一个list，新增一个map，存放值
        遍历所有map，找到最小值
        将最小值取出来min，
            if(min>0) all-min
            if(min<0) all+min

        然后将数据展示出去
